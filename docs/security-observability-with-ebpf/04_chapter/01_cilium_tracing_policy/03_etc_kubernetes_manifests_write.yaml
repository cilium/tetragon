# deny write to /etc/kubernetes/manifests
apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: "deny-write-to-etc-kubernetes-manifests"
spec:
  kprobes:
  # deny writes under /etc/kubernetes/manifests
  - call: "security_file_permission"
    syscall: false
    args:
    - index: 0
      type: "file"
    - index: 1
      type: "int"
    selectors:
    - matchPIDs:
      # match host processes
      - operator: In
        followForks: false
        isNamespacePID: true
        values:
        - 0
      # match namespace processes that are not init or its children
      - operator: NotIn
        followForks: true
        isNamespacePID: true
        values:
        - 1
      matchArgs:
      - index: 0
        operator: "Prefix"
        values:
        - "/etc/kubernetes/manifests"
      - index: 1
        operator: "Equal"
        values:
        - "2"
      matchActions:
      - action: Sigkill
