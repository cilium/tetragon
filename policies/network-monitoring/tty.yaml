apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: "tty"
spec:
  kprobes:
  - call: "tty_write"
    syscall: false
    args:
    - index: 0
      type: "kiocb"
    - index: 1
      type: "iov_iter"
      maxData: true
